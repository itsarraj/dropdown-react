import styles from '../styles/head.module.css';

const Head = () => {
    return (
        <div className={styles.head}>
            <h1>Should you use dropdown?</h1>
        </div>
    );
};

export default Head;
